# 「连接真实后端」模式：pnpm dev:real
#
# 与演示模式的区别：不再用内置演示数据，仪表盘/历史记录都读后端真实数据。
# 用法：
#   1. 先起后端：cd backend && .venv/bin/python -m uvicorn app.main:app --port 8000
#   2. 再起前端：pnpm dev:real
#
# 注意 Shopify 调用（发布、连接自检）需要后端有可用 token；
# 只读的仪表盘/历史接口不需要。

VITE_USE_MOCK=false
VITE_API_BASE=http://127.0.0.1:8000
