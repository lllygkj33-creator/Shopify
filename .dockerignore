# 构建镜像时不需要的东西。
#
# 关键：**不要把本地配置与运行期数据打进镜像** ——
# 镜像可能被推给别人，凭据和真实域名都不该在里面。

# 依赖与构建产物（镜像里重新装、重新构建）
node_modules
dist
backend/.venv
**/__pycache__
**/*.py[cod]
.pytest_cache

# 版本控制
.git
.gitignore

# 凭据（运行时用环境变量/挂载给容器，绝不进镜像）
.env
.env.real

# 说明：site.config.local.json **故意不排除**。
# 原因是前端配置在 `pnpm build` 时就被编译进 dist（import.meta.glob），
# 所以想在设备上跑真实店铺，构建时必须能读到它，否则前端只有通用占位值。
# 它已被 .gitignore 忽略，仓库里和 CI 上都不存在 → CI 出的镜像仍是通用版。
# 代价：本地用它构建出的镜像里**含你的真实域名/品牌**，绝不能推到公开仓库。

# 运行期数据（SQLite / 设置 / 历史 / 手动 token）
data

# 开发与测试产物
screenshots
coverage
**/__screenshots__
.vitest-attachments
.tanstack
.vscode
.idea
*.log
.DS_Store
