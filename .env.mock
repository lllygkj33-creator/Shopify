# 「演示数据」模式：pnpm dev:mock
#
# 内置假数据，完全不请求后端。用途：
#   - 后端没起 / 不方便连店铺时，仍然能看界面和点流程
#   - 自动化验收脚本 verify-ui.mjs 跑在演示模式下（断言不依赖真实数据）
#
# 注意：**默认不是这个模式**。`pnpm dev` 直连真实后端。
# 之所以改掉原来的默认值：演示数据是内置的假排期，如果默认开启，
# 打开仪表盘会以为「平台里已经有内容」，而其实一条都没有。

VITE_USE_MOCK=true
VITE_API_BASE=http://127.0.0.1:8000
